@model TicketCreateViewModel

@{
    ViewData["Title"] = "New Ticket";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-7">

        <!-- Header -->
        <div class="d-flex align-items-center gap-2 mb-4">
            <a asp-action="Index" class="btn btn-sm btn-light border">
                <i class="bi bi-arrow-left"></i>
            </a>
            <div>
                <h5 class="mb-0 fw-semibold">New Ticket</h5>
                <p class="text-muted small mb-0">Submit a new IT support request</p>
            </div>
        </div>

        <div class="card border-0 shadow-sm">
            <div class="card-body p-4">

                <!-- Validation Summary -->
                <div asp-validation-summary="ModelOnly"
                     class="alert alert-danger small py-2 mb-4"></div>

                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()

                    <!-- Title -->
                    <div class="mb-3">
                        <label asp-for="Input.Title" class="form-label small fw-medium">
                            Title <span class="text-danger">*</span>
                        </label>
                        <input asp-for="Input.Title"
                               class="form-control"
                               placeholder="e.g. Cannot login to CRM" />
                        <span asp-validation-for="Input.Title"
                              class="text-danger small"></span>
                    </div>

                    <!-- Description -->
                    <div class="mb-3">
                        <label asp-for="Input.Description"
                               class="form-label small fw-medium">
                            Description <span class="text-danger">*</span>
                        </label>
                        <textarea asp-for="Input.Description"
                                  class="form-control"
                                  rows="4"
                                  placeholder="Describe the issue in detail..."></textarea>
                        <span asp-validation-for="Input.Description"
                              class="text-danger small"></span>
                    </div>

                    <!-- Category + Priority -->
                    <div class="row g-3 mb-3">
                        <div class="col-md-6">
                            <label asp-for="Input.CategoryId"
                                   class="form-label small fw-medium">
                                Category <span class="text-danger">*</span>
                            </label>
                            <select asp-for="Input.CategoryId"
                                    asp-items="Model.Categories"
                                    class="form-select">
                                <option value="">-- Select Category --</option>
                            </select>
                            <span asp-validation-for="Input.CategoryId"
                                  class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Input.Priority"
                                   class="form-label small fw-medium">
                                Priority <span class="text-danger">*</span>
                            </label>
                            <select asp-for="Input.Priority" class="form-select">
                                <option value="">-- Select Priority --</option>
                                <option value="Low">🟢 Low</option>
                                <option value="Medium">🟡 Medium</option>
                                <option value="High">🟠 High</option>
                                <option value="Critical">🔴 Critical</option>
                            </select>
                            <span asp-validation-for="Input.Priority"
                                  class="text-danger small"></span>
                        </div>
                    </div>

                    <!-- Assign To + DueDate -->
                    <div class="row g-3 mb-3">
                        <div class="col-md-6">
                            <label asp-for="Input.AssignedToId"
                                   class="form-label small fw-medium">
                                Assign To
                                <span class="text-muted fw-normal">(optional)</span>
                            </label>
                            <select asp-for="Input.AssignedToId"
                                    asp-items="Model.AssignableUsers"
                                    class="form-select">
                                <option value="">-- Leave Unassigned --</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Input.DueDate" class="form-label small fw-medium">
                                Due Date
                                <span class="text-muted fw-normal">(SLA deadline)</span>
                            </label>
                            <input asp-for="Input.DueDate" type="datetime-local" class="form-control" />
                            <span asp-validation-for="Input.DueDate"
                                  class="text-danger small"></span>
                        </div>
                    </div>

                    <!-- Actions -->
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary px-4">
                            <i class="bi bi-send me-1"></i> Submit Ticket
                        </button>
                        <a asp-action="Index"
                           class="btn btn-outline-secondary px-4">
                            Cancel
                        </a>
                    </div>

                </form>
            </div>
        </div>

    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}